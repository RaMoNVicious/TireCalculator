<resources>

    <!-- Base application theme. -->
    <style name="TireTheme" parent="Theme.AppCompat.Light">
        <item name="colorPrimary">@color/app_main</item>
        <item name="colorAccent">@color/app_accent</item>
        <item name="colorPrimaryDark">@color/app_main_dark</item>
        <item name="android:colorForeground">@color/app_accent</item>
        <item name="android:textColorPrimary">@color/app_accent</item>
        <item name="android:colorBackground">@color/app_main</item>
        <item name="android:textColorPrimaryInverse">@color/app_main_dark</item>
    </style>

</resources>
